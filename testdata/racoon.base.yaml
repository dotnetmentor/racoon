config:
  parameters:
    context:
      required: true
    tenant:
      required: false
  sources:
    awsParameterStore:
      kmsKey: alias/parameter_store_key
      defaultKey: "/racoon/{context}/{key}"
